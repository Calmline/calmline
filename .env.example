# OpenAI (required for analysis)
OPENAI_API_KEY=sk-...

# Supabase (required for storage)
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Twilio Voice (live call stream)
# WebSocket server URL for Media Stream + session. Use wss:// for production, ws:// for local.
# NEXT_PUBLIC_WS_URL=wss://your-domain.com
# TWILIO_WS_BASE_URL=wss://your-domain.com
# For local: run `npm run ws-server` (default port 3001), then set NEXT_PUBLIC_WS_URL=ws://localhost:3001
